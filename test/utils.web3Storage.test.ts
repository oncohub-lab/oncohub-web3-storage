import { assert } from 'chai';

describe('web3Storage', async function () {
  it('should create client with string as token api', async function () {
    assert.equal('1', '1');
  });
  it('should throw error like: when wrong api token provided or not provided at all', async function () {
    assert.equal('1', '1');
  });
});
